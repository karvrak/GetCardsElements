
I have a html of all my cards, i need to take each card make a json file with all attribut:

{<br>
    id:<br>
    name:<br>
    type:<br>
    rarity:<br>
    RuleText:<br>
    flavorText:<br>
    pt:<br>
}
